# Legacy Files

This folder contains old/experimental frontend files that are no longer part of the canonical implementation.

These files are preserved for reference but should not be used in production.

**Canonical files are in the parent `frontend/` directory.**
